#include <stdio.h>
/**
  *main - Display alaphabets is reverse 
  *
  *Return:Always 0 (Success)
  */
int main(void)
{
	char l;

	for (l = 'z'; l >= 'a'; l--)
	{
		putchar(l);
	}
	putchar('\n');
	return (o);
}
